<UserControl x:Class="HamsterStudio.Gallery.Views.ThumbnailModeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:handy="https://handyorg.github.io/handycontrol"
             xmlns:local="clr-namespace:HamsterStudio.Gallery.Views"
             xmlns:localc="clr-namespace:HamsterStudio.Gallery.Converters"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:tkc="clr-namespace:HamsterStudio.Toolkits.Converters;assembly=HamsterStudio.Toolkits"
             d:DesignHeight="450"
             d:DesignWidth="800"
             mc:Ignorable="d">
    <DockPanel Background="AliceBlue">
        <DockPanel.Resources>
            <tkc:PathToImageSource x:Key="Path2ImgSrc500px" DecodeWidth="500" />
            <localc:GalleryFolderConverter x:Key="GalleryFolderConverter" />

            <DataTemplate x:Key="GalleryFolderFileTemplate" DataType="hfs:HamstertFileInfo">
                <Border BorderBrush="Red" BorderThickness="1" CornerRadius="5">
                    <Border.ContextMenu>
                        <ContextMenu>
                            <MenuItem Command="{Binding RevealInExplorerCommand}" Header="文件夹中打开" />
                        </ContextMenu>
                    </Border.ContextMenu>
                    <Image Margin="2" Source="{Binding FullName, Converter={StaticResource Path2ImgSrc500px}}" ToolTip="{Binding FullName}" />
                </Border>
            </DataTemplate>
        </DockPanel.Resources>
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
            <handy:Pagination Margin="5"
                              DataCountPerPage="{Binding DataCountPerPage}"
                              MaxPageCount="{Binding MaxPageCount}"
                              PageUpdated="Pagination_PageUpdated"
                              PageIndex="{Binding PageIndex, Mode=TwoWay}" />
            <TextBlock DockPanel.Dock="Top" Text="{Binding CurrentFolder.DirInfo.FullName}" />
            <CheckBox Content="自动切页" IsChecked="{Binding AutoSwitchPage}" />
            <handy:NumericUpDown Value="{Binding Columns}" />
            <handy:NumericUpDown Value="{Binding Rows}" />
            <handy:NumericUpDown Value="{Binding AutoSwitchPageMinDelay}" />
            <handy:TextBox Width="200" PreviewKeyDown="TextBox_PreviewKeyDown" Text="{Binding CurrentFolder.SearchText, Mode=TwoWay}" />
        </StackPanel>

        <!--<ItemsControl ItemTemplate="{StaticResource GalleryFolderFileTemplate}" ItemsSource="{Binding CurrentImages}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="{Binding Columns}" Rows="{Binding Rows}" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>-->

        <ItemsControl ItemTemplate="{StaticResource GalleryFolderFileTemplate}" ItemsSource="{Binding CurrentFolder.SearchedViewSource.View}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="{Binding Columns}" Rows="{Binding Rows}" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>

    </DockPanel>
</UserControl>
