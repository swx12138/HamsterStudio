<UserControl x:Class="HamsterStudio.Gallery.Views.GalleryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:handy="https://handyorg.github.io/handycontrol"
             xmlns:local="clr-namespace:HamsterStudio.Gallery.Views"
             xmlns:localc="clr-namespace:HamsterStudio.Gallery.Converters"
             xmlns:localm="clr-namespace:HamsterStudio.Gallery.Models"
             xmlns:localp="clr-namespace:HamsterStudio.Gallery"
             xmlns:localtc="clr-namespace:HamsterStudio.Toolkits.Converters;assembly=HamsterStudio.Toolkits"
             xmlns:localvm="clr-namespace:HamsterStudio.Gallery.ViewModels"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:tkc="clr-namespace:HamsterStudio.Toolkits.Converters;assembly=HamsterStudio.Toolkits"
             d:DesignHeight="450"
             d:DesignWidth="800"
             mc:Ignorable="d">
    <DockPanel>
        <DockPanel.Resources>
            <localc:GalleryFolderConverter x:Key="GalleryFolderConverter" />
            <HierarchicalDataTemplate x:Key="GalleryFolderTemplate" DataType="localm:GalleryFolderModel" ItemsSource="{Binding Folders}">
                <TextBlock VerticalAlignment="Center" Text="{Binding Converter={StaticResource GalleryFolderConverter}}" />
            </HierarchicalDataTemplate>
            <DataTemplate x:Key="GalleryFolderFileTemplate" DataType="FileInfo">
                <Grid Margin="2" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <Image HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Source="{Binding}"
                           Stretch="UniformToFill" />
                </Grid>
            </DataTemplate>
        </DockPanel.Resources>
        <TreeView DockPanel.Dock="Left"
                  ItemTemplate="{StaticResource GalleryFolderTemplate}"
                  ItemsSource="{Binding GalleryFolders}"
                  SelectedItemChanged="TreeView_SelectedItemChanged" />
        <DockPanel Background="AliceBlue">
            <TextBlock DockPanel.Dock="Top" Text="{Binding CurrentFolder.DirInfo.FullName}" />
            <handy:Pagination Margin="5"
                              HorizontalAlignment="Stretch"
                              DataCountPerPage="{Binding DataCountPerPage}"
                              DockPanel.Dock="Bottom"
                              MaxPageCount="{Binding MaxPageCount}"
                              PageUpdated="Pagination_PageUpdated"
                              PageIndex="{Binding PageIndex, Mode=TwoWay}" />
            <ItemsControl ItemTemplate="{StaticResource GalleryFolderFileTemplate}" ItemsSource="{Binding CurrentImages}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="{Binding Columns}" Rows="{Binding Rows}" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </DockPanel>
    </DockPanel>
</UserControl>
