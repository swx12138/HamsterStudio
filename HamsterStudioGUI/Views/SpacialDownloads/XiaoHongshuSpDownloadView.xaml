<UserControl x:Class="HamsterStudioGUI.Views.SpacialDownloads.XiaoHongshuSpDownloadView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:HamsterStudioGUI.Views.SpacialDownloads"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="450"
             d:DesignWidth="800"
             mc:Ignorable="d">
    <GroupBox Margin="4"
              Padding="8"
              FontSize="16"
              FontWeight="Bold"
              Header="小红书图片下载">
        <DockPanel>
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
                <TextBlock VerticalAlignment="Center" Text="图片Token：" />
                <TextBox Width="400" Text="{Binding ImageToken}" />
                <Button Command="{Binding TestCommand}" Content="Test" />
            </StackPanel>
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
                <TextBlock VerticalAlignment="Center" Text="下载状态：" />
                <TextBlock HorizontalAlignment="Center"
                           DockPanel.Dock="Top"
                           Text="{Binding Status}"
                           TextAlignment="Center" />
            </StackPanel>
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
                <TextBlock VerticalAlignment="Center" Text="可用前缀：" />
                <ItemsControl ItemsSource="{Binding KnownTokenList}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Margin="8,0"
                                       Padding="6"
                                       Background="Yellow"
                                       Text="{Binding}" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>


            <Button Margin="8" Command="{Binding DownloadCommand}" Content="下载" />
        </DockPanel>
    </GroupBox>
</UserControl>
