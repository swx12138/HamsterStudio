<UserControl x:Class="HamsterStudioGUI.Views.SpacialDownloads.BilibiliSpDownloads"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:HamsterStudioGUI.Views.SpacialDownloads"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="450"
             d:DesignWidth="800"
             mc:Ignorable="d">
    <DockPanel>
        <Grid DockPanel.Dock="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="BVID" />
                <TextBox Width="200" Text="{Binding Bvid}" />
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <TextBlock Text="BVID" />
                <TextBox Width="200" Text="{Binding Cid}" />
            </StackPanel>
            <Button Grid.Column="2"
                    Width="100"
                    Height="30"
                    Command="{Binding DownloadCommand}"
                    Content="下载" />
        </Grid>
        <Canvas />
    </DockPanel>
</UserControl>
