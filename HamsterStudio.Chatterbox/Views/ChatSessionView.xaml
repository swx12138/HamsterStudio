<UserControl x:Class="HamsterStudio.Chatterbox.Views.ChatSessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:HamsterStudio.Chatterbox.ViewModels"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HamsterStudio.Chatterbox.Views"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <DataTemplate x:Key="ContentTemplate" DataType="{x:Type vm:ChatSessionViewModel}">
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Background="#eeeeee">
                    <TextBlock Text="UserName" FontSize="12" VerticalAlignment="Center"/>
                    <hc:Badge Value="{Binding ChatInfos.Count}"
                              BadgeMargin="5,0,0,0"
                              Maximum="99"
                              HorizontalAlignment="Center" />
                </StackPanel>
                <DockPanel DockPanel.Dock="Bottom">
                    <Button Content="KB" DockPanel.Dock="Left" />
                    <Button Content="Send" DockPanel.Dock="Right" />
                    <Button Content="按住 说活" HorizontalAlignment="Stretch"/>
                </DockPanel>
                <ListBox Name="ListBoxChat"
                         Style="{StaticResource StackPanelVerticalListBox}"
                         Background="Transparent"
                         HorizontalAlignment="Stretch"
                         BorderThickness="0"
                         ItemsSource="{Binding ChatInfos}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <hc:ChatBubble MaxWidth="300" Role="{Binding Role}" Type="{Binding Type}" Content="{Binding Message}" Tag="{Binding}"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </DataTemplate>
    </UserControl.Resources>
    <ContentPresenter Content="{Binding}" ContentTemplate="{StaticResource ContentTemplate}" />
</UserControl>
